export function getSaludo(nombre) {
  return "Hola " + nombre;
}

/* El test suite va a ejecutar todo este archivo, por eso el clg con la var nombre no tiene mucho sentido mostrar su valor con ese clg */
/* console.log(`Este es un texto: ${getSaludo(nombre)}  `); */
